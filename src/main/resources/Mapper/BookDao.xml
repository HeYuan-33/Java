<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Mapper.BookDao">

    <!-- 查询图书列表，根据图书名称模糊查询 -->
    <select id="getList" resultType="Entity.Book">
        SELECT * FROM book
        WHERE bookname LIKE CONCAT('%', #{bookname}, '%');
    </select>

    <!-- 插入图书 -->
    <insert id="insertBook" parameterType="Entity.Book">
        INSERT INTO book (bookname, bookisbn, publisher, price, imgurl)
        VALUES (#{bookname}, #{bookisbn}, #{publisher}, #{
        price}, #{imgurl});
    </insert>

</mapper>
